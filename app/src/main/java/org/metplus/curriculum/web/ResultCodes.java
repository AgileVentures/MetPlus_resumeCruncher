package org.metplus.curriculum.web;

/**
 * Enumeration with all the result codes
 */
public enum ResultCodes {

    SUCCESS,
    ACCESS_DENIED,
    FATAL_ERROR,
    RESUME_NOT_FOUND,
    JOB_ID_EXISTS,
    JOB_NOT_FOUND;
}
